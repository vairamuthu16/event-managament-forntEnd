import { Link } from 'react-router-dom';

export default function AuthBox({title,submit,fields,set,err,button,register,adminLink}){return <div className="container py-16 max-w-md"><form className="card p-7" onSubmit={submit}><h1 className="text-3xl font-black">{title}</h1>{err&&<div className="bg-red-50 text-red-700 p-3 rounded-lg mt-4">{err}</div>}{register&&<input className="field mt-5" placeholder="Full name" required value={fields.name} onChange={e=>set({...fields,name:e.target.value})}/>}<input className="field mt-4" type="email" placeholder="Email" required value={fields.email} onChange={e=>set({...fields,email:e.target.value})}/><input className="field mt-4" type="password" placeholder="Password" required value={fields.password} onChange={e=>set({...fields,password:e.target.value})}/>{register&&<select className="field mt-4" value={fields.role} onChange={e=>set({...fields,role:e.target.value})}><option value="attendee">I want to attend events</option><option value="organizer">I want to organize events</option></select>}<button className="btn btn-primary w-full mt-5">{button}</button><p className="text-sm text-gray-500 mt-4 text-center">{register?'Already registered? ':'New here? '}<Link className="text-brand font-bold" to={register?'/login':'/register'}>{register?'Sign in':'Create account'}</Link></p>{adminLink&&<p className="text-sm text-center mt-3"><Link className="text-brand font-bold" to="/admin/login">Admin login →</Link></p>}</form></div>}
